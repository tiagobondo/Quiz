import {
    View,
    Text
} from 'react-native'

import {styles} from './style'

export default function Options(){
  return(
    <View style={styles.container}>
      <Text>Options</Text>
    </View>
  )}

export {Options}