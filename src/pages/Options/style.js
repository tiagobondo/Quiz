import {StyleSheet} from 'react-native'

const styles = StyleSheet.create({
  container: {
    flex:1,
    backgroundColor:'#12a4d9',
    alignItems:'center',
    padding: 10,
  },

  textOptions: {
    fontSize: 16,
    color: '#0009',
    textAlign:'justify',
    marginBottom: 10
  }
})

export {styles}